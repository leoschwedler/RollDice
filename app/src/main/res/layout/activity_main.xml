<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/fundodois"
    tools:context=".MainActivity">

    <ImageView
        android:id="@+id/rollDice"
        android:layout_width="wrap_content"
        android:layout_height="180dp"
        android:layout_marginBottom="56dp"
        android:clickable="true"
        android:src="@drawable/btn"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.495"
        app:layout_constraintStart_toStartOf="parent" />

    <TextView
        android:id="@+id/textViewOverlay"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:clickable="false"
        android:focusable="false"
        android:layout_centerInParent="true"
        android:fontFamily="@font/blod"
        android:text="roll dice"
        android:textColor="#AD715E"
        android:textSize="35sp"
        app:layout_constraintBottom_toBottomOf="@+id/rollDice"
        app:layout_constraintEnd_toEndOf="@+id/rollDice"
        app:layout_constraintStart_toStartOf="@+id/rollDice"
        app:layout_constraintTop_toTopOf="@+id/rollDice" />

    <ImageView
        android:id="@+id/dice"
        android:layout_width="193dp"
        android:layout_height="166dp"
        android:layout_marginTop="112dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/diceum" />

</androidx.constraintlayout.widget.ConstraintLayout>
